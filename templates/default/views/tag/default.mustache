{{<html}}
{{$main}}

<div class="wrapper">

    <h4>POSTS TAGGED:</h4> 

    <h1>{{page.title}}</h1>

    {{& page.contents.html}}
    
    {{#empty(page.posts)}}
    No post for this tag.
    {{/empty(page.posts)}}
    {{^empty(page.posts)}}
    <div class="grid grid-321">
    {{#page.posts}}
        {{> partials.post}}
    {{/page.posts}}
    </div>
    {{/empty(page.posts)}}

</div>

{{/main}}
{{/html}}
