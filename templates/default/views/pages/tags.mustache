{{<html}}
{{$main}}

<div class="wrapper tags-page">

    <h1 class="page-title">All tags</h1>

    {{& page.contents.html}}

    {{#empty(context.tags)}}
    Empty.
    {{/empty(context.tags)}}
    {{^empty(context.tags)}}
    <div class="grid grid-543">
    {{#context.tags}}
        {{> partials.tag}}
    {{/context.tags}}
    
    </div>
    {{/empty(context.tags)}}

</div>

{{/main}}
{{/html}}
